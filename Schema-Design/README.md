## Schema Design

This step creates the base tables used for performance tuning experiments.

No primary keys or foreign keys are defined intentionally to observe Oracle optimizer behavior without automatic index creation.
